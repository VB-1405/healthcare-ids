Just a base project for a bigger goal. Intrusion Detection System with AI-Based capabilities is the final goal with AI having enough knowledge to compare false positives and true positives to make the work load of the analyst easier.


Will keep updating this in some time.

This is a WIP(Work in Progress) Project. The end goal is to build an IDS which can make queries on it's own, check the IP addresses directly on CTI's like VirusTotal, AbuseIPDB and more. In the end the goal is to reduce the work for security analyst by reducing the number of False Positives using the Datasets from various IDS on different sorts of network attacks.
